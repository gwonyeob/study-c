#include<stdio.h>
#include<string.h>
int main()
{
	char a[10002] = { 0 };
	char b[10002] = { 0 };
	char result[10003] = { 0 };
	int A, B = 0;
	int sum = 0;
	int i = 0;
	int len = 0;
	scanf_s("%s", a, sizeof(a));
	scanf_s("%s", b, sizeof(b));
	len = (strlen(a) < strlen(b)) ? strlen(a) : strlen(b);
	for (i = 0; i < len; i++)
	{
		a[len-i-1]
	}
}
//??????